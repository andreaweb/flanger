<section>
  <div>
    <div>
      <div>

        <h2>Change your password</h2>

        <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
          <%= f.error_notification %>

          <%= f.input :reset_password_token, as: :hidden %>
          <%= f.full_error :reset_password_token %>

          <div>
            <div>
            <%= f.input :password, label: "New password", required: true, autofocus: true, input_html: { class: ""}, wrapper: false, label_html: { class: "" }, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
            </div>
          </div>

          <div>
            <div>
            <%= f.input :password_confirmation, label: "Confirm your new password", input_html: { class: ""}, wrapper: false, label_html: { class: "" }, required: true %>
            </div>
          </div>

          
            <%= f.button :submit, "Change my password", class:"" %>

        <% end %>

        <%= render "devise/shared/links" %>

      </div>
    </div>
  </div>
</section>